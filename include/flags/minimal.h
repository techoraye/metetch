#pragma once

/**
 * Minimal Display Mode - Compact System Information
 * Displays essential system info in a compact format
 */

#include "data_types.h"

/**
 * Render minimal/compact system information display
 * Shows only essential information on a few lines
 */
void renderMinimalDisplay(const Info& inf);
